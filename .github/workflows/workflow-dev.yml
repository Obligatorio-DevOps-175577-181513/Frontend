name: Build on DEV

on:
  push:
    branches:
      - branch-develop
  repository_dispatch:
    types:
      - build-dev
  workflow_dispatch:

concurrency:
  group: environment-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-dev:
    uses: <Organization>/<repoName>/.github/workflows/deploy-to-s3.yml@develop
    with:
      CI_ENV: develop
      CONTENTFUL_ENVIRONMENT: 'develop'
    secrets:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: ${{ secrets.AWS_REGION }}
      S3_ORIGIN_BUCKET: ${{ secrets.S3_ORIGIN_BUCKET_DEV }}

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#     - name: Checkout repository
#       uses: actions/checkout@v2

#     - name: Set up Node.js
#       uses: actions/setup-node@v2
#       with:
#         node-version: '14'

#     - name: Install dependencies
#       run: npm install

#     - name: Build project
#       run: npm run build

#     - name: Deploy to S3
#       uses: jakejarvis/s3-sync-action@v0.5.1
#       with:
#         args: --follow-symlinks --delete
#       env:
#         AWS_S3_BUCKET: dev-frontend-175577-181513
#         AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
#         AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#         AWS_REGION: us-east-1
#         SOURCE_DIR: dist/apps/catalog/



# name: Build DEV

# on: workflow_call
#   inputs:
#     CI_ENV:
#       required: true
#       type: string
#     CONTENTFUL_ENVIRONMENT:
#       required: true
#       type: string
#   secrets:
#     AWS_ACCESS_KEY_ID:
#       required: true
#     AWS_SECRET_ACCESS_KEY:
#       required: true
#     AWS_SESSION_TOKEN:
#       required: true
#     AWS_DEFAULT_REGION:
#       required: true
#     S3_ORIGIN_BUCKET:
#       required: true

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     steps:
#       - uses: actions/checkout@v2

#       - name: Setup Node.js
#         uses: actions/setup-node@v2
#         with:
#           node-version: '16.x'
#           cache: 'npm'

#       - name: Install dependencies
#         run: npm ci

#       - name: Run Linters
#         run: npm run linters-ci

#       - name: Build React static site
#         run: ./scripts/build.sh
#         env:
#           CI_ENV: ${{ inputs.CI_ENV }}
#           CONTENTFUL_ENVIRONMENT: ${{ inputs.CONTENTFUL_ENVIRONMENT }}